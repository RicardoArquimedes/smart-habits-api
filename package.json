{
  "name": "smart-habits-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "compile": "tsc",
    "prebuild": "rm -rf dist && tsc && cp package.json dist/",
    "build": "repack-zip",
    "postbuild": "mkdir -p build && mv smart-habits-api.zip build/ && rm dist/package.json"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0",
    "google-auth-library": "^10.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "repack-zip": "^0.2.8",
    "typescript": "^5.3.3"
  }
}
